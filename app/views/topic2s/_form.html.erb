<%= form_for(@topic2) do |f| %>
  <% if @topic2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic2.errors.count, "error") %> prohibited this topic2 from being saved:</h2>

      <ul>
      <% @topic2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
