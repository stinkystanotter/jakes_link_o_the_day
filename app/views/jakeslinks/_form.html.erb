<%= form_for(@jakeslink) do |f| %>
  <% if @jakeslink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jakeslink.errors.count, "error") %> prohibited this jakeslink from being saved:</h2>

      <ul>
      <% @jakeslink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
